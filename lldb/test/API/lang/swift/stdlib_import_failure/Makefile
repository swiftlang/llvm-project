SWIFT_SOURCES := main.swift
# With explicit modules LLDB loads the stdlib from the recorded path and never
# consults the module search paths.
SWIFT_ENABLE_EXPLICIT_MODULES := NO

include Makefile.rules
